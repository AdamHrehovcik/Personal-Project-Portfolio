/.quarto/
/_site/
.DS_Store

# NOTE: /docs/ is intentionally NOT ignored — it's the rendered output
# GitHub Pages serves from (see README for the "Publishing" section).

**/*.quarto_ipynb

# Local-only source material that shouldn't be published: commercially
# licensed data (MSCI/FX, the energy forecasting competition dataset), a
# trained model too large for a public repo (65MB), and a team slide deck
# whose content already lives on the NLP project page. Kept locally for
# anyone re-running the notebooks, never committed.
/_source-data/
